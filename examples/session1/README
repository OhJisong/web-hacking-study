# 🟦 수업 목표 (이번 주)

웹의 기본 동작 구조(클라이언트-서버, HTTP 요청/응답 등)를 이해한다.

Flask의 기본 구조(라우트, 템플릿, 정적파일)를 손으로 구현해본다.

JavaScript의 핵심(요청, DOM, 이벤트, Same-Origin Policy)을 이해한다.

XSS(교차 스크립팅)의 개념을 이해하고, 간단한 취약점 실습(Reflected XSS)까지 해본다.
